# 스윙바이 — 작업 안내

Godot 4 + GDScript로 만드는 모바일 물리 퍼즐. **모든 명세는 `docs/PLAN.md`(v4)에 있다.**
이 파일은 그 문서의 요약이자 색인이다. 충돌하면 `docs/PLAN.md`가 우선한다.

## 작업 규칙 (PLAN §0)

1. 마일스톤(§17) 순서대로 진행한다. 한 마일스톤이 끝나면 **멈추고** 요약 후 다음 지시를 기다린다.
2. 마일스톤 시작 시 계획을 먼저 짧게 제시한다. 문서와 다른 판단이 필요하면 **구현 전에 이유와 함께 묻는다.**
3. `core/`, `tools_shared/`, `monetization/`은 **노드·씬·`Vector2`·렌더링 API 금지.** 게임과 검증 도구가 같은 코드를 공유한다.
4. 물리·충돌·레벨 수치를 바꾸는 변경은 회귀 테스트(§16.5)를 통과해야 한다. **테스트 기준값을 코드에 맞춰 고치지 않는다.**
5. 마일스톤마다 커밋. 메시지는 한국어로 `M2: 조준·발사 입력 구현` 형식.
6. 사용자에게 보이는 문구는 전부 한국어이며 §13의 문구를 그대로 쓴다.
7. **스테이지 좌표를 손으로 쓰지 않는다.** 생성기(§8.4) 또는 에디터로 만들고 검증기(§8.5)를 통과시킨다.
8. 광고 단위 ID·서명 키·스토어 계정 정보를 저장소에 넣지 않는다.

## 발사대 행성 (PLAN §5.9) — v4.1에서 추가

우주선은 `start`의 한 점이 아니라 **반경 `PAD_R`(22) 표면에 서 있다가 법선 방향으로 이륙**한다.

```
발사 좌표 = start + PAD_R · (cos θ, sin θ)
발사 속도 = speed  · (cos θ, sin θ)
θ 범위    = 돔 중심 방향(start → goal) ± ARC(90°)
```

- `PAD_R`을 24 이상으로 키우면 1-4의 성공 폭이 11.25°→6.50°로 무너진다. **바꾸지 않는다.**
- 조준 기준점은 우주선이 아니라 **돔 중심**이다. 화면 어디를 눌러도 조준된다(§10.1).
- 빈 곳 드래그 화면 이동은 없다. 큰 맵은 미니맵으로만(§10.3).

## 정밀도 규칙 (PLAN §2.1) — 가장 자주 깨지는 규칙

Godot의 `Vector2`는 **32비트**다. 시뮬레이션 상태를 여기 담으면 부록 A의 검증값이 어긋난다.

- `core/`, `tools_shared/`에서 `Vector2`, `deg_to_rad()`, `TAU`, `distance_to()`, `length()` **금지**
- 상태는 `PackedFloat64Array` 또는 분리된 `float` 변수 (GDScript `float`은 double)
- 거리는 `sqrt(dx*dx + dy*dy)`
- `n * DT`는 **곱셈 유지.** 누산(`ft += DT`)으로 바꾸면 오차가 쌓인다

확인: `./run.sh check`

## 명령어 (PLAN §2.3)

```
./run.sh bench                        성능 관문 (§17 M0)
./run.sh solve [단계ID]                전 각도 스캔 (§16.1)
./run.sh validate [단계ID|장번호]        전체 검증 (§16.3) — 커밋 전 필수
./run.sh gen --recipe <파일> --slot <칸>  후보 생성 (§16.4)
./run.sh test                         gdUnit4 (§16.5)
```

`GODOT` 환경변수로 실행 파일 지정: `GODOT=~/bin/godot4 ./run.sh bench`

## 폴더 (PLAN §3)

| 경로 | 내용 | 비고 |
|---|---|---|
| `core/` | 물리 코어 (§5) | ★ 노드·Vector2 금지 |
| `tools_shared/` | 스캔·지표·생성 알고리즘 (§8) | ★ 노드 금지, CLI와 에디터 공용 |
| `tools/` | headless CLI (`SceneTree` 스크립트) | 배포 제외 |
| `levels/data/` | 단계 JSON, 파일 하나당 한 단계 (§6.1) | 좌표를 손으로 쓰지 않는다 |
| `game/`, `scenes/`, `render/`, `ui/` | 게임 실행부 (§9~§13) | |
| `monetization/` | 광고 정책 순수 함수 (§14) | ★ 노드 금지 |
| `platform/` | 광고 제공자, 진동, 생명주기, dp 환산 (§15.2) | |
| `addons/swingby_editor/` | 스테이지 에디터 (§8.7) | **배포 제외** |
| `docs/reference/` | 부록 A~C의 Node 참조 구현 | 이식 대조용 |

## 고정 버전

| 항목 | 버전 | 정한 날 |
|---|---|---|
| Godot | **미정** — 착수 시 최신 안정 버전으로 고정할 것 | |
| gdUnit4 | **미설치** | |
| AdMob 플러그인 | **미정** — M9에서 Godot 버전과 호환되는 것으로 (§14.2) | |

> Godot 버전을 올릴 때는 **AdMob 플러그인 호환을 먼저 확인한다**(커뮤니티 유지보수라 lag이 있다).

## 진행 상황

- [x] **M0** 프로젝트 세팅과 성능 관문 — 정확성 통과, 속도 상한 4.90초. 사용자 환경에서 `./run.sh bench` 확정 필요
- [x] **M1** 물리 코어와 검증 도구 — 6단계 이식, solve, 테스트 3종. Godot 3 하네스로 §6.2 전부 재현 확인
- [x] **M2** 최소 플레이 (1-1) — 코드 작성 완료. **실기기 확인은 사용자 몫**
- [ ] M3 익스포트 · M4 큰 맵 · M5 위험 요소 · M6 화면 흐름
- [ ] M7 검증기·에디터 · M8 생성기 · M9 광고 · M10 40단계 · M11 다듬기 · M12 출시 · M13 후반부

## 지금 열려 있는 것

1. **M2 실기기 확인** — 에디터에서 F5 로 1-1 이 뜨는지, 조준 감도(§10.0 dp 환산)가
   자연스러운지. 여기서는 Godot 4 를 실행할 수 없어 화면을 한 번도 못 봤다
2. `./run.sh bench`를 Godot 4에서 돌려 §17 M0 판단표를 확정 (≤2초 / 2~5초)
3. gdUnit4 설치 후 `addons/gdUnit4/` 배치
4. §8.9.1 공전 단계 생성 비용 문제 — M8에서 결정
5. §10.3 — 큰 맵에서 미니맵만으로 항로 파악이 되는지 M4에서 실기기 확인
